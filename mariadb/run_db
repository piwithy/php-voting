# start db

set -e
set -x

# first, if the /var/lib/mariadb directory is empty, unpack it from our predefined db
[ "$(ls -A /var/lib/mariadb)" ] && echo "Running with existing database in /var/lib/mysql" || ( echo 'Populate initial db'; tar xpzvf default_mysql.tar.gz )

/usr/sbin/mysqld